# Example configuration file,
# Before plugging in the power,
# place this file on a Micro SD card
# and insert the card into the unit

# File format: <name>=<value>
# Fields for the example sketch:
#  hello = string to print in loop()
#  doDelay = delay waitMs in loop()
#  wait = time (milliseconds) to wait after printing hello

prepulse_readings_cfg=10
heatpulse_length_cfg=4
sleep_after_pulse_cfg=60
postpulse_readings_cfg=10
heater_setting_cfg=18
lora_active_cfg=false
sleepcycle_setting_cfg=1500